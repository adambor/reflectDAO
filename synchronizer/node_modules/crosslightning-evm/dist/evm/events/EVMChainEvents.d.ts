import { EVMSwapData } from "../swaps/EVMSwapData";
import { ChainEvents, EventListener } from "crosslightning-base";
import { EVMSwapProgram } from "../swaps/EVMSwapProgram";
import { providers } from "ethers";
export declare class EVMChainEvents implements ChainEvents<EVMSwapData> {
    private readonly listeners;
    private readonly directory;
    private readonly provider;
    private readonly evmSwapProgram;
    constructor(directory: string, provider: providers.Provider, evmSwapProgram: EVMSwapProgram);
    private getLastHeight;
    private saveLastBlockheight;
    private processEvent;
    private checkEvents;
    init(): Promise<void>;
    registerListener(cbk: EventListener<EVMSwapData>): void;
    unregisterListener(cbk: EventListener<EVMSwapData>): boolean;
}
